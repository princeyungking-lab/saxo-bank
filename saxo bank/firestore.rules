rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all collections (test mode - open for demo)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
